apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dmp.fullname" . }}-sso-http
  labels:
    {{- include "dmp.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: sso-http
spec:
  replicas: {{ .Values.sso.http.replicaCount }}
  selector:
    matchLabels:
      {{- include "dmp.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: sso-http
  template:
    metadata:
      labels:
        {{- include "dmp.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: sso-http
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- range .Values.global.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      containers:
        - name: sso-http
          image: {{ include "dmp.image" (dict "imageRoot" .Values.sso.http.image "global" .Values.global) }}
          imagePullPolicy: {{ .Values.sso.http.image.pullPolicy }}
          {{- if .Values.sso.http.livenessProbe.enabled }}
          livenessProbe:
            {{- if .Values.sso.http.livenessProbe.httpGet }}
            httpGet:
              path: {{ .Values.sso.http.livenessProbe.httpGet.path }}
              port: {{ .Values.sso.http.livenessProbe.httpGet.port }}
            {{- else if .Values.sso.http.livenessProbe.tcpSocket }}
            tcpSocket:
              port: {{ .Values.sso.http.livenessProbe.tcpSocket.port }}
            {{- end }}
            initialDelaySeconds: {{ .Values.sso.http.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.sso.http.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.sso.http.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.sso.http.livenessProbe.failureThreshold }}
            {{- if .Values.sso.http.livenessProbe.successThreshold }}
            successThreshold: {{ .Values.sso.http.livenessProbe.successThreshold }}
            {{- end }}
          {{- end }}
          {{- if .Values.sso.http.readinessProbe.enabled }}
          readinessProbe:
            {{- if .Values.sso.http.readinessProbe.httpGet }}
            httpGet:
              path: {{ .Values.sso.http.readinessProbe.httpGet.path }}
              port: {{ .Values.sso.http.readinessProbe.httpGet.port }}
            {{- else if .Values.sso.http.readinessProbe.tcpSocket }}
            tcpSocket:
              port: {{ .Values.sso.http.readinessProbe.tcpSocket.port }}
            {{- end }}
            initialDelaySeconds: {{ .Values.sso.http.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.sso.http.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.sso.http.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.sso.http.readinessProbe.failureThreshold }}
            {{- if .Values.sso.http.readinessProbe.successThreshold }}
            successThreshold: {{ .Values.sso.http.readinessProbe.successThreshold }}
            {{- end }}
          {{- end }}
          {{- if .Values.sso.config.enabled }}
          envFrom:
            - configMapRef:
                name: {{ include "dmp.fullname" . }}-sso-config
          {{- end }}
          ports:
            {{- range $key, $value := .Values.sso.http.service.ports }}
            - name: {{ $key }}
              containerPort: {{ $value }}
              protocol: TCP
            {{- end }}
          {{- if and .Values.sso.config.enabled .Values.sso.config.files }}
          volumeMounts:
            - name: sso-config-volume
              mountPath: {{ .Values.sso.config.mountPath }}
          {{- end }}
      {{- if and .Values.sso.config.enabled .Values.sso.config.files }}
      volumes:
        - name: sso-config-volume
          configMap:
            name: {{ include "dmp.fullname" . }}-sso-config
            items:
            {{- range $key, $value := .Values.sso.config.files }}
              - key: {{ $key }}
                path: {{ $key }}
            {{- end }}
      {{- end }}
